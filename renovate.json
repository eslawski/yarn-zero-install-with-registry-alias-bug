{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "packageRules": [
    {
      "matchPackageNames": ["webpack", "node"],
      "groupName": "Example PR that does NOT update yarn cache"
    },
    {
      "matchPackageNames": ["vue"],
      "groupName": "Example PR that correctly updates yarn cache"
    }
  ]
}
